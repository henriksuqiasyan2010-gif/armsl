# Python 3.9–3.12 (mediapipe не поддерживает 3.13+).
# Версии зафиксированы точно и соответствуют проверенному рабочему
# окружению (Python 3.11.9), в котором проходят все тесты.
mediapipe==1.0.1
opencv-python==5.0.0.93
numpy==2.4.6
scikit-learn==1.9.0
matplotlib==3.11.1
pandas==3.0.5
pytest==9.1.1

# tensorflow нужен только на этапе обучения модели (src/train.py).
# Версию НЕ фиксируем заранее: в текущем окружении он не установлен,
# а значит не проверен — точную версию подберём и впишем сюда, когда
# дойдём до обучения (она должна ужиться с numpy 2.4.6 и Python 3.11).
# Пока строка закомментирована: pip install -r requirements.txt его
# не поставит, и запуск train.py упадёт на импорте — это ожидаемо.
# tensorflow==<определится на этапе обучения>
