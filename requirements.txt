# Python 3.9–3.12 (mediapipe не поддерживает 3.13+).
# Версии ниже соответствуют фактически проверенному рабочему окружению
# (Python 3.11.9). Ограничение numpy<2 снято: актуальные версии mediapipe
# работают с numpy 2.x.
mediapipe>=1.0.1
opencv-python>=5.0.0.93
numpy>=2.0
scikit-learn>=1.9.0
matplotlib>=3.11.1
pandas>=3.0.5
pytest>=8.3.5

# Нужен только на этапе обучения модели (src/train.py).
# В текущем окружении ещё не установлен — версию сверим, когда дойдём до обучения.
tensorflow>=2.18.0
