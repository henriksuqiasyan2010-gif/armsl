# Python 3.9–3.12 (mediapipe не поддерживает 3.13+).
# Версии зафиксированы точно и соответствуют проверенному рабочему
# окружению (Python 3.11.9), в котором проходят все тесты.
mediapipe==1.0.1
opencv-python==5.0.0.93
numpy==2.4.6
scikit-learn==1.9.0
matplotlib==3.11.1
pandas==3.0.5
pytest==9.1.1

# tensorflow нужен только на этапе обучения модели (src/train.py).
# Установлен и проверен: совместим с numpy==2.4.6 (требует numpy>=1.26.0,
# конфликта нет — pip не менял версию numpy при установке) и Python 3.11.9.
# GRU-путь в train.py реально прогнан с этой версией (не только baseline).
tensorflow==2.21.0
